<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("./partials/head") %>
    <title>Home - Node Narrate</title>
    <style>
        :root {
            --node-green: #00ff7f;
            --node-dark-bg: #0d1117;
            --node-darker: #010409;
            --card-glow: rgba(0, 255, 127, 0.2);
            --font-gray: #d0d0d0;
            --btn-glow: 0 0 8px rgba(0, 255, 127, 0.6);
            --border-color: #30363d;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            padding: 0;
            background-color: var(--node-dark-bg);
            font-family: 'Fira Code', monospace;
            color: var(--font-gray);
            position: relative;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .grid-pattern {
            position: fixed;
            inset: 0;
            background-image: linear-gradient(
                    to right,
                    rgba(255, 255, 255, 0.03) 1px,
                    transparent 1px
                ),
                linear-gradient(
                    to bottom,
                    rgba(255, 255, 255, 0.03) 1px,
                    transparent 1px
                );
            background-size: 40px 40px;
            z-index: -1;
        }

        .terminal-header {
            background-color: var(--node-darker);
            border: 1px solid var(--border-color);
            color: var(--node-green);
            padding: 1rem 2rem;
            margin: 2rem auto 1rem;
            border-radius: 10px;
            font-size: 1.2rem;
            width: fit-content;
            box-shadow: 0 0 12px var(--card-glow);
        }

        .container {
            max-width: 1300px;
            margin: 0 auto;
            padding: 2rem 1rem;
        }

        .blog-wrapper {
            display: flex;
            flex-wrap: wrap;
            gap: 2rem;
            justify-content: center;
        }

        .blog-card {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid var(--border-color);
            border-left: 3px solid var(--node-green);
            border-radius: 12px;
            width: 300px;
            backdrop-filter: blur(6px);
            box-shadow: 0 0 12px var(--card-glow);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .blog-card:hover {
            transform: translateY(-6px);
            box-shadow: 0 0 16px var(--node-green);
        }

        .blog-card img {
            width: 100%;
            height: 180px;
            object-fit: cover;
            border-bottom: 1px solid var(--border-color);
        }

        .card-body {
            padding: 1rem;
        }

        .card-title {
            color: var(--node-green);
            font-size: 1.2rem;
            margin-bottom: 0.6rem;
        }

        .card-text {
            font-size: 0.9rem;
            color: var(--font-gray);
            max-height: 70px;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .btn {
            display: inline-block;
            background: transparent;
            border: 1px solid var(--node-green);
            color: var(--node-green);
            padding: 0.4rem 1rem;
            font-weight: bold;
            border-radius: 6px;
            margin-top: 1rem;
            text-decoration: none;
            transition: all 0.2s ease;
            box-shadow: var(--btn-glow);
        }

        .btn:hover {
            background-color: var(--node-green);
            color: #0d1117;
            box-shadow: 0 0 12px var(--node-green);
        }

        /* Scrollbar Styling */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-thumb {
            background: var(--node-green);
            border-radius: 10px;
        }

        ::selection {
            background: var(--node-green);
            color: #000;
        }
    </style>
</head>

<body>
    <%- include("./partials/nav") %>

    <div class="grid-pattern"></div>

    <div class="terminal-header">
        <span style="color: #888;">$</span> node narrate --home
    </div>

    <div class="container">
        <div class="blog-wrapper">
            <% blogs.forEach(blog => { %>
                <div class="blog-card">
                    <img src="<%= blog.coverImageUrl %>" alt="Cover Image">
                    <div class="card-body">
                        <h5 class="card-title"><%= blog.title %></h5>
                        <p class="card-text"><%= blog.body %></p>
                        <a href="/blog/<%= blog._id %>" class="btn">View Blog</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <%- include("./partials/script") %>
    <%- include("./partials/Footer") %>
</body>

</html>
